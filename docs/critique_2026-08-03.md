# Critique complète du projet Cadence — 03/08/2026

Revue menée à la demande de Benoît, **sans aucune correction de code**. Lecture intégrale de
`src/engine/`, `src/config/franceTravailConfig.ts`, `src/types/`, `src/storage/`, `api/`, des
composants qui affichent un statut ou un montant, et confrontation à `CLAUDE.md` / `docs/SPEC.md`.

Filtre de priorité appliqué : tout ce qui menace l'un des deux devoirs sacrés (perte de données,
chiffre/statut faux) est 🔴, même si l'occurrence semble rare.

État au moment de la revue : 627 tests verts, `tsc -b` propre, commit `759e0ec`.

---

## 🔴 Priorité élevée

### 1. ✅ RÉSOLU (03/08/2026) — Une lecture ratée du stockage efface définitivement toutes les données

> **Corrigé.** `chargerDonnees` renvoie désormais trois issues distinctes (`ok` / `vide` /
> `illisible`) au lieu d'un état vide indifférencié ; le verrou d'écriture d'`App.tsx` porte sur cet
> état et non plus sur un `useRef`. Une lecture illisible affiche un écran bloquant (texte brut
> téléchargeable et copiable, détail technique, restauration de la copie de secours, « repartir de
> zéro » gaté par une case à cocher) et **ne déclenche aucune écriture**. Une copie de secours
> (`cadence:v1:donnees.backup`) conserve la version précédant chaque écriture réussie.
> Preuve : test de régression `components/__tests__/App.donneesIllisibles.test.tsx` (échouait avant :
> la clé passait de 876 octets à un état vide sans aucun clic), 13 tests d'adaptateur, démonstration
> navigateur du 03/08/2026. Description d'origine conservée ci-dessous.

**Preuve** : `src/storage/localStorageAdapter.ts:207-221` (`chargerDonnees` renvoie `donneesVides`
si le parse Zod échoue) + `src/App.tsx:56-67` (le `useEffect` de sauvegarde se déclenche dès que
`donnees` change **et que `chargementTermine.current` est vrai**, ce qui est le cas juste après le
chargement).

**Pourquoi c'est un risque** : si les données stockées deviennent illisibles pour une raison
quelconque (un champ dans un format inattendu, une écriture interrompue, une future évolution de
schéma mal anticipée), l'app démarre comme si l'utilisateur était nouveau — puis **réécrit
immédiatement cet état vide par-dessus les vraies données**. L'original est perdu, sans copie de
secours, sans message : seule une ligne dans la console du navigateur, que personne ne lit. C'est la
violation la plus directe possible du devoir n°1, et elle ne demande aucune action de l'utilisateur
pour se produire.

**Action suggérée** : avant toute réinitialisation, recopier la valeur brute dans une clé de
quarantaine (`cadence:v1:donnees.corrompu.<horodatage>`) et ne jamais écraser la clé principale tant
que l'utilisateur n'a pas vu un écran explicite lui proposant d'exporter la sauvegarde ou de
repartir de zéro. Bloquer l'écriture automatique tant que ce choix n'est pas fait.

---

### 2. ⚠️ FILET MINIMAL POSÉ (03/08/2026), SUJET OUVERT — Un échec d'écriture du stockage est totalement silencieux

> **Partiellement traité, volontairement.** `sauvegarderDonnees` renvoie maintenant
> `{ ok } | { ok: false, message }` au lieu de laisser l'exception se perdre, et `App.tsx` affiche un
> bandeau rouge non refermable (« Tes dernières modifications n'ont PAS été enregistrées ») invitant
> à exporter immédiatement. Ce filet a été ajouté dans le même correctif que le point 1 parce que la
> copie de secours double l'espace occupé et rendait donc la saturation plus probable — il aurait été
> irresponsable de créer ce risque en laissant l'échec invisible.
> **Ce point n'est PAS clos.** Restent à traiter : le comportement quand le stockage est réellement
> plein (que purger, dans quel ordre), l'export de secours automatique, la détection en amont de la
> saturation, et le sort de la copie de secours dans ce cas.
> **Deux consommateurs d'espace ajoutés par le correctif du point 1, à intégrer à ce chantier** :
> `cadence:v1:donnees.backup` (une copie du jeu de données, renouvelée à chaque écriture réelle) et
> `cadence:v1:donnees.illisible` (une copie, écrite seulement lors d'un « repartir de zéro » et
> **jamais purgée ensuite**). Les deux sont bornés à une copie chacun — aucune accumulation, c'est
> vérifié par test — mais ils portent l'occupation à trois copies au pire. Description d'origine
> ci-dessous.

**Preuve** : `src/storage/localStorageAdapter.ts:223-225` — `sauvegarderDonnees` appelle
`localStorage.setItem` sans `try/catch` ; `src/App.tsx:65` l'appelle sans `await` ni `.catch()`.

**Pourquoi c'est un risque** : si le stockage du navigateur est plein, ou si le mode privé/les
réglages du navigateur bloquent l'écriture, l'erreur part dans le vide. L'utilisateur continue de
saisir ses contrats pendant des semaines en croyant que tout est enregistré, et découvre la perte à
la fermeture de l'onglet. Devoir n°1.

**Action suggérée** : capturer l'erreur et afficher un bandeau bloquant et persistant
(« tes dernières modifications n'ont PAS été enregistrées »), avec un bouton d'export JSON immédiat.

---

### 3. ✅ RÉSOLU (03/08/2026) — Deux moteurs de calcul concurrents pour le même tableau mensuel, avec un ordre différent

> **Corrigé par fusion.** `engine/calculerSerie.ts` et `engine/franchises.ts` sont **supprimés** :
> `engine/indemnisationMensuelle.ts` est désormais le moteur unique du tableau mensuel, de
> `lib/totalAreAnnuel.ts` (point 16) et de `evaluerRisqueTropPercu`. Plus deux chemins, donc plus
> d'écart possible.
>
> **L'ordre est tranché par le texte, pas par un arbitrage de goût.** Deux sources officielles
> concordantes, trouvées le 03/08/2026 :
> - **Annexe X** au règlement annexé à la convention du 15/11/2024, **article 23 §1er** :
>   « L'application des dispositions des articles 21 et 22 s'effectue dans l'ordre suivant : différé
>   d'indemnisation, délai d'attente, franchise de congés payés, franchise. »
> - **Guide France Travail, p.12** : « …dans l'ordre suivant : un délai d'attente, une franchise
>   mensuelle congés payés, une franchise mensuelle salaires. »
>
> C'est l'ordre de `indemnisationMensuelle.ts` ; celui de `franchises.ts` était l'inverse. Le TODO de
> `franchises.ts` invoquait le relevé du 14/04/2026 (« franchise CP 4 / différé 5 / travail 19 ») pour
> justifier son ordre — mais **les chiffres de ce relevé disent l'inverse de ce que le commentaire en
> avait conclu** : une fois le mois d'ouverture correctement fenêtré (point 21), l'ordre du guide
> reproduit les trois chiffres exactement, l'ordre inverse donne « délai 7 / CP 2 ». Le relevé de
> janvier (« 2 jours de délai ») tranche dans le même sens. Le point 3 affirmait « aucun cas réel ne
> distingue les deux ordres » : c'est faux depuis le 03/08/2026, deux mois consécutifs les distinguent.
>
> **Précision trouvée au passage, corrigée aussi** — guide p.17, étape 6 : l'ordre officiel a CINQ
> étapes, et le *reliquat* reporté des mois antérieurs se déduit **après** la franchise salaires du
> mois courant, pas fondu dans le plafond mensuel comme le faisait le code. Sans effet tant que la
> franchise salaires est inactive (les deux formulations sont alors identiques) ; l'écart porterait
> sinon sur la répartition entre les deux franchises, donc sur le reliquat qui fonde le trop-perçu.
>
> Preuve : `src/lib/__tests__/lignesRevenusMensuels.test.ts`, 641 tests verts, `tsc -b` propre, build
> propre, vérifié en navigateur. Description d'origine conservée ci-dessous.

**Preuve** : `src/engine/indemnisationMensuelle.ts:130-147` consomme *délai d'attente puis franchise
congés payés* ; `src/engine/franchises.ts:36-40` (via `src/engine/calculerSerie.ts:78`) consomme
*franchise congés payés puis délai*. Les deux fichiers portent chacun un TODO
(`franchises.ts:5-9`, `calculerSerie.ts:110-116`) qui reconnaît l'écart et indique que **le guide
officiel confirme l'ordre inverse de celui implémenté dans `franchises.ts`**.
`src/components/RevenusMensuels.tsx:232` — le seul écran qui affiche ce tableau — utilise le moteur
dont l'ordre est documenté comme contredit par le guide.

**Pourquoi c'est un risque — motif corrigé le 03/08/2026 après mesure.** La formulation d'origine
(« deux montants en euros différents ») était **fausse**, et je la corrige ici plutôt que de la
laisser : mesuré sur **7056 scénarios**, les deux ordres donnent **exactement les mêmes jours
indemnisés**, donc les mêmes montants. Il y a une raison mathématique : pour un mois donné,
`min(a, dispo) + min(b, dispo − min(a, dispo)) = min(a + b, dispo)`, expression **symétrique en a et
b** — l'ordre ne peut pas changer le total consommé dans le mois.

Le risque réel est ailleurs, et il est confirmé : **l'ordre change la RÉPARTITION de ce total entre le
délai d'attente et la franchise congés payés.** Mesuré sur 1400 scénarios de fin de période avec
réserves non épuisées : **0 divergence sur les jours indemnisés, 1016 divergences sur les soldes
finaux.** Exemple :

```
franchise CP totale = 2, disponibilités mensuelles = [0, 0, 2]
  A (délai→CP, ordre du guide) : franchise CP restante = 2   délai restant = 5
  B (CP→délai, celui affiché)  : franchise CP restante = 0   délai restant = 7
```

C'est déterminant parce que la règle sourcée fonde le trop-perçu sur le reliquat de **franchises**, et
non sur le délai d'attente (cf. section « 2026-08-03 — Trop-perçu » de ce document) : A conclut
« 2 jours de franchise non soldés → risque de trop-perçu », B conclut « franchise épuisée → aucun
risque ». **L'ordre décide donc de l'existence d'un trop-perçu.**

Aggravant : `evaluerRisqueTropPercu` s'appuie sur **A**, le tableau mensuel affiche **B**. L'app peut
donc montrer « franchise CP soldée » dans le tableau à côté d'un bandeau « risque de trop-perçu
avéré » ailleurs. Devoir n°2 — 🔴 maintenu.

**Action suggérée** : le chantier de fond est la fusion des deux moteurs, **et il doit englober le
traitement du mois d'ouverture partiel** (cf. points 21 et 12 bis) — à spécifier séparément.
⚠️ Le filet minimal envisagé le 03/08 (faire autorité à A sur les colonnes « Délai » / « Franchise
CP ») a été **validé puis abandonné après mesure** : il produirait un tableau consommant plus de
franchise que la notification n'en accorde, et il changerait les montants. Détail et motifs de rejet
des deux autres options : point 12 bis.

---

### 4. ✅ RÉSOLU (03/08/2026) — L'écran des revenus mensuels annonce une franchise salaires qu'il ne déduit pas

**Preuve** : `src/components/RevenusMensuels.tsx:485-488` affiche « Franchise salaires : X jours à
déduire de ton indemnisation », mais la colonne des jours indemnisés vient de `calculerSerie`
(ligne 232), un moteur qui **ne connaît pas du tout la franchise salaires** (cf.
`src/engine/calculerSerie.ts`, aucune notion de franchise salaires).

**Pourquoi c'est un risque** : l'écran dit à l'utilisateur qu'il faut retrancher X jours, puis
affiche des montants qui ne retranchent rien — deux chiffres contradictoires sur le même écran, dont
au moins un est faux. **Ce cas est devenu atteignable aujourd'hui même** : avant l'ajout du champ
déclaratif `franchiseSalairesTotale` (commit `3e0d3c8`), cette valeur était toujours inconnue et le
bloc ne s'affichait jamais. Devoir n°2, et c'est une régression introduite par le dernier chantier.

**Action suggérée** : même correction que le point 3 — un seul moteur. Tant que ce n'est pas fait,
soit masquer ce bloc, soit y écrire explicitement que la déduction n'est pas répercutée dans le
tableau.

---

### 5. ✅ RÉSOLU (03/08/2026) — Le badge « Sécurité » s'affiche sur une simple projection, pas sur un droit acquis

**Preuve** : `src/engine/prediction.ts:105-106` — si la date de franchissement *projetée au rythme
passé* tombe avant l'échéance, le niveau passe à `securite`.
`src/components/ProjectionChart.tsx:25` affiche alors littéralement le mot **« Sécurité »**, en vert.

**Pourquoi c'est un risque** : « Sécurité » se lit comme « c'est bon, tes droits sont assurés ».
Or il ne s'agit que d'une extrapolation du rythme passé : si l'utilisateur ne signe rien de plus, il
n'atteindra pas les 507 h. C'est exactement le « faux feu vert » que le devoir n°2 interdit — d'autant
que le même badge sert au cas où les heures sont réellement acquises, sans distinction visuelle.

**Action suggérée** : distinguer visuellement « acquis » (heures réellement travaillées, ou contrats
déjà signés) de « en bonne voie » (projection). Réserver le vert au premier.

---

### 6. ✅ RÉSOLU (03/08/2026) — Le badge « Bloqué » s'affiche à 30 jours de l'échéance même s'il ne manque presque rien

**Preuve** : `src/engine/prediction.ts:107-108` — dès que `joursRestants <= 30`
(`SEUIL_JOURS_ANNIVERSAIRE_IMMINENT`, ligne 12) et que la projection ne suffit pas, le niveau bascule
à `bloque`, quel que soit le nombre d'heures manquantes.

**Pourquoi c'est un risque** : un artiste à qui il manque 10 h à 25 jours de son échéance (soit un
seul cachet) se voit afficher **« Bloqué »** en rouge, alors que la situation est parfaitement
rattrapable. Le devoir n°2 nomme explicitement le « faux "Bloqué" ». Le risque est concret : quelqu'un
peut renoncer à chercher un cachet parce que l'app lui dit que c'est fini.

**Action suggérée** : ne basculer en `bloque` que si le nombre d'heures manquantes est réellement
hors de portée dans le temps restant (par exemple : plus d'heures qu'un rythme maximal plausible ne
permet d'en faire), ou introduire un état intermédiaire « urgent mais possible ».

---

### 7. ✅ RÉSOLU (03/08/2026) — L'alerte « plafond de cachets dépassé » peut se déclencher à tort

**Preuve** : `src/engine/decompteHeures.ts:170-173` — tous les cachets d'un contrat sont attribués au
mois de `contrat.date`, c'est-à-dire au mois de **fin** du contrat. L'alerte est levée dans
`src/engine/alertes.ts:167-177`. Le projet dispose pourtant déjà de la répartition au prorata
(`src/engine/decoupageMensuel.ts`, `repartirContratParMois`), utilisée ailleurs mais pas ici.

**Pourquoi c'est un risque** : une tournée de 20 cachets à cheval sur deux mois voit ses 20 cachets
comptés dans le seul mois de fin. Combinée à un autre contrat, l'app peut annoncer « 30 cachets en
mars, au-delà du plafond de 28 » alors que la réalité est 15 + 15. C'est une **fausse alerte**,
nommée telle quelle dans le devoir n°2, et elle pousse l'utilisateur à « corriger » une saisie qui
était juste.

**Action suggérée** : construire `cachetsParMois` avec `repartirContratParMois` plutôt qu'avec le
mois de fin. À vérifier au passage : la règle officielle attribue-t-elle les cachets au prorata ou à
la date de fin ? Ne pas coder avant de le savoir.

---

### 8. L'endpoint d'import IA est ouvert à tous, sans authentification ni limite

**Preuve** : `api/extract-document.ts:724-737` — le handler accepte n'importe quel POST. Le TODO
ligne 729 le dit : « vérifier ici que la requête vient d'un utilisateur premium valide ». Aucune
vérification d'origine, aucun quota, et **aucun contrôle de taille côté serveur** : la limite de
3 Mo n'existe que côté navigateur (`src/lib/fichierImportIA.ts`), donc contournable par n'importe qui
appelant l'URL directement.

**Pourquoi c'est un risque** : une fois déployé, n'importe qui connaissant l'adresse peut envoyer des
documents à volonté et faire tourner la facture Mistral sur ta clé. Ce n'est pas théorique : les
endpoints d'API publics sont scannés automatiquement. Risque financier direct et indisponibilité du
service pour toi.

**Action suggérée** : ne pas déployer publiquement en l'état. Au minimum : contrôle d'origine, quota
par IP/session, et contrôle de taille côté serveur avant tout appel à Mistral.

---

### 9. Le texte de consentement affirme une chose qui n'est pas encore vraie

**Preuve** : `src/content/mentionEnvoiIA.ts:28` et `:39` — le texte montré à l'utilisateur avant
chaque envoi affirme « Ces documents ne sont pas utilisés pour entraîner les modèles de Mistral ».
Le commentaire du même fichier (lignes 12-18) reconnaît que la clé **n'est pas encore** sur le plan
payant qui garantit cela, et le backlog de `CLAUDE.md` porte toujours l'item ⬜ « Basculer
MISTRAL_API_KEY sur le plan payant ».

**Pourquoi c'est un risque** : c'est une affirmation fausse faite à l'utilisateur au moment précis où
il donne son consentement, sur des documents contenant des données personnelles sensibles (numéro de
sécurité sociale, date de naissance, employeurs). Double exposition : devoir n°2, et consentement
RGPD obtenu sur une information inexacte.

**Action suggérée** : soit basculer la clé sur le plan payant avant tout usage réel, soit corriger le
texte immédiatement pour qu'il décrive la situation réelle. Ne pas laisser cet écart ouvert.

---

### 10. Un JSON importé peut injecter des valeurs absurdes sans qu'aucun contrôle ne s'y oppose

**Preuve** : `src/storage/localStorageAdapter.ts:45-48` — `nbCachets`, `nbHeures`, `nbJoursEEE` et
`salaireBrut` sont validés par un simple `z.number()`, sans minimum. Un salaire négatif, 900 cachets
sur un contrat d'un jour, ou des heures négatives passent la validation et alimentent directement le
salaire de référence (`src/engine/salaireReference.ts:36`) et le décompte des 507 h.

**Pourquoi c'est un risque** : l'import JSON est une fonctionnalité mise en avant (« récupérer le
fichier d'un ami », cf. SPEC §11.A). Un fichier édité à la main ou corrompu produit alors une
allocation journalière et un statut entièrement faux, présentés comme fiables. Devoir n°2. À noter
que le projet a déjà rencontré exactement ce scénario avec `dateNaissance: "19994-06-09"` — le
correctif a porté sur ce seul champ, pas sur les montants.

**Action suggérée** : ajouter des bornes de plausibilité au schéma (`.min(0)` partout, et un maximum
raisonnable sur les cachets/heures par contrat), sur le chemin d'écriture uniquement — surtout pas
sur le chemin de lecture, pour ne pas transformer une donnée existante en faux « données perdues »
(cf. la note déjà présente dans `lib/coherenceProfil.ts`).

---

### 11. Contradiction non résolue sur le plafond ARE (déjà tracée)

**Preuve** : `src/config/franceTravailConfig.ts:70-90` — la config retient 174,80 / 177,56 /
181,18 € (source Unédic), alors que le guide France Travail éd. juillet 2026 écrit « ne peut pas
dépasser 174,80 € depuis le 1er janvier 2024 ».

**Pourquoi c'est un risque** : deux sources officielles se contredisent sur une constante qui borne
l'allocation. L'arbitrage pris (Unédic) est documenté et défendable, mais non confirmé. L'effet ne se
manifeste qu'à un salaire de référence très élevé, ce qui limite fortement la portée réelle.

**Action suggérée** : point déjà ouvert en backlog priorité haute — contact direct Unédic ou France
Travail. Rien à faire dans le code d'ici là.

---

## 🔴 Découvert APRÈS la revue, en instruisant les points 3 et 4 (03/08/2026)

> Numéroté 21 pour ne pas décaler les numéros 1-20, sur lesquels les prompts s'appuient.

### 21. ✅ RÉSOLU (03/08/2026) — Le mois d'ouverture partiel est mal traité par les DEUX moteurs — bug CONFIRMÉ, mais pas affiché aujourd'hui

> **Corrigé.** Le mois d'ouverture n'est plus ni sauté par le moteur ni recalculé comme un mois
> entier par l'affichage : il est simulé sur sa **vraie fenêtre**, `dateOuverture` → fin du mois.
> Principe validé par Benoît le 03/08/2026 (« la période qui suit la réadmission est l'intersection
> entre mois civil et date anniversaire »), et appuyé par l'**exemple 9 du guide France Travail p.13** :
> un droit ouvert le 19/12/22 fait courir le délai d'attente « du 19 au 25/12/22 », pas du 1er au 7.
> Le règlement, lui, est **muet** sur la proratisation du premier mois — la démonstration tient donc
> sur cet exemple officiel plus les 4 mois certifiés, pas sur un article.
>
> Effet mesuré sur les 62 contrats réels : janvier 2026 passe de 31 jours / 129 h / 16 jours non
> indemnisables à **14 jours / 93 h / 12**, et le total annoncé de 14 961,77 € tombe à **14 286,84 €** —
> **674,93 € d'ARE faux retirés** (janvier 309,94 € + février 364,99 €), sur les deux seuls mois que
> les relevés chiffrent à 0. Les 4 mois certifiés sont désormais reproduits **sans aucun écart**, ce
> qu'aucune des deux implémentations antérieures ne faisait.
>
> ⚠️ La phrase « pas affiché aujourd'hui » du titre était vraie au moment de la revue et a cessé de
> l'être le jour même : la récupération des données (point 22) a remis les contrats en base et le faux
> montant est apparu à l'écran. Corrigé quelques heures plus tard.
>
> La décision « Q1 » du moteur (ne pas calculer ce mois) n'est pas contredite dans son fondement :
> France Travail fait bien deux passes séparées sur ce mois, et Cadence ne devine toujours pas celle
> de l'ancien droit. Elle est resserrée — Cadence calcule la passe du **nouveau** droit, dont tous les
> paramètres sont connus, et le dit explicitement à l'utilisateur.

**Preuve — mesure sur données réelles** (`docs/cadence-import-complet.json`, ouverture le 18/01/2026,
franchise CP totale 5 j, délai 7 j ; `calculerSerieDepuisContrats` + reproduction exacte du chemin de
`RevenusMensuels.construireLignesAffichage`) :

| Mois | Certifié France Travail | B (affiché) | A |
|---|---|---|---|
| 2026-02 | **0** | **7** ✗ | 0 ✓ |
| 2026-03 | 17 | 17 ✓ | **16** ✗ |
| 2026-04 | 18 | 18 ✓ | **17** ✗ |
| 2026-05 | 29 | 29 ✓ | 29 ✓ |

**Aucun des deux moteurs ne reproduit les 4 mois certifiés sur le chemin de production.** Le test
certifié ne passe que parce qu'il alimente `calculerSerieIndemnisation` à la main avec un solde de
mi-parcours (délai 5, CP 5, report 2) qui encode déjà janvier — voir aussi le point 12 bis ci-dessous
sur le trou de validation.

**Cause racine : janvier.** Le droit s'ouvre le 18, donc seuls 14 jours de janvier relèvent du nouveau
droit, et le relevé réel montre que janvier n'a consommé que **2 jours de délai**. Or :
- `calculerSerie` (B) traite janvier comme un **mois entier de 31 jours** et en consomme **9**
  (7 de délai + 2 de franchise CP) — approximation documentée en tête de `calculerSerie.ts`, mais
  dont la conséquence chiffrée n'avait jamais été mesurée ;
- `calculerSerieDepuisContrats` (A) **ignore** janvier (`moisDebutCalcul = moisSuivant(moisOuverture)`)
  et redémarre en février avec `franchiseCPRestante: franchiseCPTotale` et
  `delaiRestant: delaiAttenteInitial`, soit les totaux **intacts** — il n'en consomme donc **rien**.

**Pourquoi c'est un risque** : le tableau affiche **7 jours indemnisés en février au lieu de 0**, soit
environ **376 €** annoncés en trop (7 × 53,81 €). Devoir n°2, sur un montant, pas sur un statut.

**VÉRIFIÉ le 03/08/2026 — le chiffre faux n'est PAS à l'écran aujourd'hui.** Lecture directe du
`localStorage` de production (`cadence-benoit3.vercel.app`, via le navigateur réel) et relecture du
stockage d'Edge : le profil est présent (ouverture 18/01/2026, AJ 53,81 €, franchise 5 j, délai 7 j)
mais **`contrats: []` et `periodes: []`**, et l'écran « Revenus mensuels » en est encore à « à partir
de quel mois ? ». Aucun tableau n'est donc calculé. La cause n'est pas que le calcul serait bon, mais
qu'il n'y a plus rien à calculer (cf. point 22). **Le faux montant apparaîtra au premier clic sur
« Commencer le suivi ».**

**Précision utile pour le correctif** : les 376 € valent exactement 7 × 53,81 €, et 53,81 € est l'AJ
inscrite dans le profil seul. Le bug se **reproduit donc sans aucun contrat**, à partir du profil et
de la date d'ouverture — c'est reproductible en test sans dépendre des 56 contrats.

**Action suggérée** : le traitement correct du mois partiel touche la décision actée sur
l'indécomposabilité des mois de régularisation — c'est un arbitrage produit, pas un correctif
technique. Principe proposé (validé par personne à ce jour) : **compter en jours, pas en mois** —
fenêtre du mois = intersection entre le mois civil et la période de droits (janvier 2026 = 14 jours,
du 18 au 31), franchise et délai consommés chronologiquement dans cette fenêtre, débordement sur le
mois suivant explicite. Deux invariants à verrouiller par test : la somme des jours indemnisés depuis
l'ouverture ne dépasse jamais les jours calendaires écoulés ; franchise et délai sont consommés
exactement une fois.

### 12 bis. ✅ SANS OBJET (03/08/2026) — Le filet « faire autorité à A sur les colonnes » a été validé puis INVALIDÉ par la mesure

Traçé ici parce que la décision a été prise et annulée dans le même fil, et que le raisonnement
compte pour la suite. Filet envisagé : faire venir les colonnes « Délai » et « Franchise CP » du
tableau depuis A (sourcé, ordre du guide) au lieu de B, en laissant `jours indemnisés` et les
montants à B. Validé par Benoît, puis **abandonné avant toute implémentation** pour deux raisons
mesurées :

1. **Arithmétiquement impossible** : janvier de B (2 j de franchise CP) + la suite de A (5 j) =
   **7 jours consommés sur 5 notifiés**. Le tableau contredirait la notification.
2. **L'argument « aucun montant ne bouge » était FAUX.** Il reposait sur l'identité symétrique
   `min(a,d) + min(b, d−min(a,d)) = min(a+b, d)`, qui ne vaut qu'à **soldes de départ égaux**. A et B
   ne partent pas des mêmes soldes (cf. point 21) : février passerait de 7 à 0 jours indemnisés,
   mars de 17 à 16, avril de 18 à 17.

Deux autres options ont été explicitement **rejetées par Benoît**, avec leurs motifs :
- *avertissement seul sous le tableau* — « une colonne fausse avec un avertissement dessous reste une
  colonne fausse, ça ne satisfait pas le devoir n°2 » ;
- *afficher « indéterminé » en cas de désaccord* — « à 1016/1400, ce serait un signal permanent, donc
  un signal ignoré ».

Conséquence : **il n'existe pas de filet minimal viable tant que le mois partiel n'est pas tranché.**
Les points 3, 4 et 21 se rejoignent sur cette même racine, et le chantier de fusion des deux moteurs
doit englober le traitement du mois partiel.

### 12 ter. Trou de validation de `calculerSerieDepuisContrats` — documents attendus

Aucun test ne valide la fonction réellement utilisée en production contre les mois certifiés : seule
sa machine à états interne (`calculerSerieIndemnisation`) l'est, et avec un solde saisi à la main.
Benoît récupère les pièces. Ce qu'il faut, par ordre d'importance :

1. **Les contrats réels depuis le 24/03/2025** — date de début, date de fin, type, cachets et/ou
   heures, salaire brut. C'est l'entrée du moteur, le morceau indispensable : `calculerSerieDepuisContrats`
   ne prend pas des heures mensuelles, il prend des contrats et dérive les mois lui-même
   (`repartirContratParMois`) — et c'est précisément cette dérivation qui n'est pas testée.
2. **Les justificatifs de déclaration mensuelle** — ils donnent les heures du mois *telles que France
   Travail les a comptées*, donc la référence attendue en sortie de `repartirContratParMois`.
   **Seuls, ils ne ferment pas le trou** ; combinés au point 1, oui.
3. **La notification d'ouverture de mars 2025** — date d'ouverture, franchise CP totale, délai.

Vérifié le 03/08/2026 : aucun fichier `Justificatif_declaration_*.pdf` n'existe sur la machine
(balayage du profil utilisateur entier).

**Avancée du 03/08/2026** : Benoît a fourni, depuis ses relevés officiels France Travail, trois
valeurs certifiées — 24/03/2026 ONPL 7 cachets / 731,16 € ; 20/04/2026 Les Arts Phocéens 6 cachets /
653,00 € ; et une troisième référence (4 cachets / 933,90 €, Les Arts Phocéens) qui correspond en
réalité au contrat du **15→18/06/2026**, déjà présent et déjà en cachets dans les deux fichiers. Les
relevés existent donc sous une forme ou une autre : ils restent à récupérer en entier.

### 12 quater. ✅ RÉSOLU (03/08/2026) — Le test « certifié » ne valide pas le chemin de production — dette distincte

Tracé séparément de 12 ter à la demande de Benoît, parce que ce n'est pas le même manque. 12 ter dit
qu'il manque des *données* d'entrée ; 12 quater dit que le test existant est **structurellement
incapable** de prouver quoi que ce soit sur la production : il alimente `calculerSerieIndemnisation`
à la main avec un solde de mi-parcours (délai 5, CP 5, report 2) qui **encode déjà le résultat de
janvier**. Il valide donc un calcul qui démarre *après* le bug.

Ce qu'il faudra dans le vrai correctif : un test qui **part d'une notification d'ouverture réelle**
(date d'ouverture, franchise CP totale, délai) et **déroule toute la chaîne** jusqu'aux lignes
affichées, sans aucun solde pré-mâché. Tant que ce test n'existe pas, un « certifié » vert ne prouve
rien sur le mois d'ouverture.

---

## 🔴 Incident du 03/08/2026 — perte de données constatée

### 22. ✅ RÉSOLU (03/08/2026) — Les contrats de l'utilisateur ont disparu de l'application

**Constat, mesuré** : le `localStorage` de production et celui d'Edge contiennent un profil valide
mais **zéro contrat et zéro période**. Benoît confirme avoir vu ses contrats à l'écran auparavant.
C'est le devoir sacré n°1.

**Reconstitution — il n'y a jamais eu un seul jeu de données, mais deux, en parallèle** :

| Lignée | Contrats | Période couverte | Dernière trace |
|---|---|---|---|
| A — « courant / à venir » | 11 → 21 → 25 | 08/01/2026 → 31/01/2027 | 02/08/2026 15:23 |
| B — « historique importé » | 56 | 11/01/2025 → 30/06/2026 | 31/07/2026 18:17 |

Aucune des deux ne contient l'autre : 38 contrats propres à B, 7 propres à A, 18 communs (appariés
sur dateDébut + dateFin). **Sur ces 18, aucun écart de montant ni de date** — seulement les
identifiants (normal, imports distincts), l'orthographe de l'employeur, et **3 contrats dont le type
de rémunération diffère** (cachets dans B, heures dans A). Les relevés France Travail tranchent en
faveur de **B (cachets)** pour le 24/03 et le 20/04.

**Ce qui est établi sur l'origine de la perte** :
- Aucun fichier `cadence-sauvegarde-avant-import-2026-08-02*` n'existe, alors que l'app en crée un à
  chaque import (il en existe pour les 24/07, 26/07 et 31/07). Le passage de B à A n'est donc **pas**
  passé par la fonction d'import.
- L'hypothèse la plus probable — **non démontrée** — est le travail en parallèle sur plusieurs
  origines : l'historique de navigation montre `localhost:5173`, `5174`, `5175`, `5177` et cinq
  adresses Vercel distinctes (`cadence-benoit3`, `cadence-git-master`, `cadence-kfgelhf98`,
  `cadence-lsu0135qf`, `cadence-seven-gilt`). Chaque origine a son propre stockage.
- Le passage de A (25 contrats, présents le 02/08 à 15h23) à zéro tombe dans la fenêtre du point 1,
  corrigé le 03/08.

**Ce qui est récupérable — tout** (vérifié, lecture seule) :
- Lignée B : `docs/cadence-import-complet.json` (= `Téléchargements/cadence-sauvegarde-avant-import-2026-07-31 (2).json`)
- Lignée A : `Téléchargements/cadence-export-2026-08-02 (2).json`

Décompression Snappy complète du LevelDB d'Edge et de Chrome (décodeur écrit pour l'occasion,
7 blocs compressés lus) : **le maximum retrouvé dans un navigateur est 56 contrats**. Aucune origine,
aucun port, aucun déploiement ne détient plus que ce qui est déjà dans les fichiers ci-dessus.
Historique des fichiers Windows non configuré ; points de restauration et clichés VSS non
consultables sans droits administrateur (donc non exclus, mais sans objet ici).

**Fusion faite et importée le 03/08/2026** — `docs/cadence-fusion-2026-08-03.json`, **62 contrats**
du 01/01/2025 au 31/01/2027 (les 56 de B, moins les 2 lignes du 26/06, plus la ligne unique de A,
plus les 7 contrats d'enseignement à venir propres à A).

Les deux arbitrages en attente, tranchés :

- **Contrat du 26/06 (Les Étoiles du Classique)** — les deux lignées donnent le MÊME total (26 h,
  420 €) : B le scinde en deux lignes (14 h à 0 € + 1 cachet à 420 €), A le porte en une seule ligne
  mixte (14 h ET 1 cachet). **Retenu : la ligne unique de A**, décidé par Benoît, conforme au
  justificatif officiel de juin qui écrit « 14h et 1 cachet, une seule activité » (cf.
  `docs/reprise.md`) ; elle porte en plus `source: import_pdf` et `statutVerification: confirme`.
- **Profil** — ni A ni B n'est complet, mais sur le seul champ où ils se contredisent, B est juste.
  Contre-épreuve au moteur réel, mêmes 62 contrats, seule `dateAnniversairePrecedente` change :
  `2025-03-23` (B) reconstitue le cycle **2025-03-24 → 2026-01-17, 780 h** — exactement la fenêtre de
  la Notification 2 de France Travail (FCT 17/01/2026, 299 j), déjà verrouillée par
  `engine/__tests__/cycles.test.ts`. `2026-01-17` (A) fabrique un cycle **2026-01-18 → 2026-01-17**,
  0 h, clôturé : un cycle qui n'existe pas. Et c'est précisément l'unique entrée que A portait dans
  `exercicesGeles` — ce n'était pas une donnée, c'était l'erreur d'un an gravée dans le stockage.
  Retenu : **profil de B** (borne + les deux AJ historiques) **+ le taux PAS et
  `soldeIndemnisationDepart` de A**, `exercicesGeles` remis à vide. Ça clôt au passage l'écart
  journal/fichier signalé dans `docs/reprise.md` : c'est le fichier qui avait raison.

Normalisation appliquée dans la foulée : `« LEVALLOIS »` → `« Commune de Levallois Perret »` (le
renommage qui n'avait jamais persisté, cf. `docs/reprise.md` — il fait échouer la correspondance à
l'import PDF), plus la casse de « Les Étoiles du Classique » et « ONPL ». Aucun effet sur les calculs.

**Vérifié à l'écran** (Chrome réel de Benoît, `https://cadence-git-master-benoit3.vercel.app/`, après
import par le vrai chemin `importerJSON`) : 62 contrats en stockage, **588 h / 507 h** au tableau de
bord, exercice en cours 2026-01-18 → 2027-01-17 à 588 h, exercice précédent **2025-03-24 → 2026-01-17
à 780 h** (pas de cycle fantôme), récurrence Levallois intacte (7 contrats, 2026-07 → 2027-01).
L'app a re-figé d'elle-même les deux exercices clos corrects. Deux recoupements indépendants sur les
mois déjà certifiés par les justificatifs : mars 2026 = 21 h + 7 cachets = 105 h / 1 196,16 € ;
juin 2026 = 35 h + 11 cachets = 167 h.

**Cause à retenir pour la suite** : `localStorage` est cloisonné par origine, et le travail s'est
étalé sur cinq adresses Vercel et quatre ports locaux. La seule adresse stable est celle de
**branche** (`cadence-git-master-benoit3.vercel.app`, toujours le dernier `master`) ; les URLs de
déploiement changent à chaque build et repartent d'un stockage vide. C'est là, et nulle part
ailleurs, que vivent les données réelles.

### 25. Le plafond de cumul 118 % du PMSS n'est jamais appliqué

> Trouvé le 03/08/2026 en cherchant la source officielle de la méthode de calcul (chantier des
> moteurs). Numéroté 25 pour ne pas décaler 1-20.

**Preuve** : `src/config/franceTravailConfig.ts` porte `plafondCumulCoeffPMSS: 1.18` et
`pmssMensuel: 4005` — mais le commentaire de ce dernier le dit lui-même : « **Toujours non lu par le
moteur** (plafond de cumul, module V2 non construit, cf. SPEC.md §11.B) ». Vérifié : aucun appel dans
`src/engine/` ni `src/lib/`.

**La règle** — guide France Travail p.17, étape 5 : « Le montant total de vos rémunérations cumulé au
montant de l'ARE à verser ne doit pas dépasser 118 % du plafond mensuel de la sécurité sociale. […]
Si le cumul est supérieur au plafond : le montant mensuel de l'ARE à verser est recalculé = Montant du
plafond − rémunérations brutes mensuelles. »

**Pourquoi c'est un risque** : sur un mois à forte activité, Cadence annonce une ARE que France
Travail écrêtera. Devoir n°2, sur un montant. Le plafond vaut 1,18 × 4 005 = **4 725,90 €** en 2026.

**Portée réelle sur les données de Benoît : nulle aujourd'hui.** Son mois le plus chargé du droit en
cours est juin 2026 (2 472,90 € de contrats + 484,29 € d'ARE = 2 957,19 €), soit à 1 768 € du
plafond. Le risque ne se matérialise qu'à un mois nettement plus chargé — mais il est certain à ce
moment-là.

**Action suggérée** : appliquer l'écrêtement dans le moteur unique
(`engine/indemnisationMensuelle.ts`), qui dispose déjà de `salairesContratsBruts` par mois — tout ce
qu'il faut est en place. Attention : le guide précise que le nombre de jours indemnisables est alors
recalculé (« arrondi à l'entier supérieur ») depuis le montant écrêté, ce n'est pas qu'un plafond sur
l'euro affiché.

### 23. ✅ RÉSOLU (03/08/2026) — On ne peut pas restaurer une sauvegarde sans d'abord créer un profil

> Trouvé le 03/08/2026 en restaurant les données du point 22. Numéroté 23 pour ne pas décaler 1-20.

**Constat** : le bouton « Importer » (et « Exporter ») n'existe que dans l'écran principal, donc
seulement une fois l'onboarding franchi (`App.tsx` — la barre d'actions est rendue après le garde
`profil`). Un utilisateur qui vient de tout perdre voit l'écran « Bienvenue sur Cadence » et **doit
inventer un profil avant de pouvoir récupérer le vrai**. Constaté en conditions réelles : il a fallu
ressaisir date de naissance, situation, date anniversaire et borne précédente — quatre champs que le
fichier de sauvegarde contenait déjà — juste pour atteindre le bouton d'import.

**Pourquoi ça compte** : c'est le chemin de récupération du devoir sacré n°1. Le filet existe (export
JSON, sauvegarde automatique avant import), mais il est inaccessible au moment précis où on en a le
plus besoin. Le risque concret est qu'un utilisateur pressé remplisse l'onboarding « pour voir »,
commence à saisir des contrats à la main, et ne pense à l'import qu'après — l'import écrasant alors
un travail déjà fait.

**Action suggérée** : offrir l'import depuis l'écran d'onboarding lui-même (« J'ai déjà une
sauvegarde Cadence »), en court-circuit du formulaire. Aucun impact sur le moteur.

### 26. Le plafond de 28 cachets/mois ne plafonne rien — il ne sert qu'à afficher un avertissement

> Trouvé le 03/08/2026 en corrigeant le point 7. Numéroté 26 pour ne pas décaler 1-20.
> **Non sourcé : aucune correction de code ne doit être tentée avant d'avoir tranché la règle.**

**Constat** : `config.plafondCachetsParMois` (28, marqué ✅ Annexe 10) n'est lu qu'à trois endroits, et
aucun ne plafonne un décompte — vérifié exhaustivement : `engine/alertes.ts` (alerte
`plafond_cachets_mois`), `components/ContractForm.tsx` (avertissement de saisie) et
`engine/prediction.ts` (borne d'atteignabilité ajoutée au point 6). Le décompte des 507 h
(`engine/decompteHeures.ts`) ne s'en sert **jamais** : 40 cachets déclarés sur un mois comptent
40 × 12 = 480 h, sans écrêtement.

Pourtant `engine/decoupageMensuel.ts:8-9` affirme le contraire — « Pas de plafond mensuel appliqué
ici (ex. 28 cachets/mois = 336 h) : **ce plafond gouverne l'affiliation aux 507 h**
(`decompteHeures.ts`) ». Or `decompteHeures.ts` ne l'applique pas. Des deux, l'un se trompe.

**Pourquoi ça compte** : si la vraie règle est que les cachets au-delà de 28 dans un mois civil ne
comptent pas pour l'affiliation, alors Cadence **surcompte les heures** de tout mois à forte activité,
et peut afficher un « Sécurité » vert sur des 507 h qui ne sont pas acquises — un faux feu vert, le
devoir sacré n°2. Si au contraire le plafond ne borne que le nombre de cachets *déclarables*, le code
est juste et c'est le commentaire de `decoupageMensuel.ts` qu'il faut corriger.

**Portée sur les données de Benoît : nulle aujourd'hui.** Vérifié le 03/08/2026 sur les 62 contrats
restaurés : le mois le plus chargé est mars 2025 avec 20 cachets, sous le plafond de 28. Aucun mois ne
l'approche.

**Action suggérée** : trancher sur le guide France Travail / l'Annexe 10 avant tout code. `docs/SPEC.md:534`
range déjà la « proratisation du plafond de cachets sur mois partiel » parmi les cas de bord non
traités — c'est la même zone d'ombre, à instruire d'un seul coup. Ne rien changer sans source.

---

## 🟡 Correction mineure

### 12. Le salaire de référence attribue chaque contrat à son seul mois de fin

**Preuve** : `src/engine/salaireReference.ts:34` et `src/engine/decompteHeures.ts:140` — un contrat
est retenu si `c.date` (sa date de **fin**) tombe dans la fenêtre, et alors il compte **en entier**.

**Pourquoi c'est un risque** : un contrat commencé avant le début de la fenêtre mais terminé dedans
apporte 100 % de son salaire et de ses heures, alors qu'une partie du travail est antérieure. Le
projet sait pourtant découper au prorata (`decoupageMensuel.ts`). **À vérifier avant toute
correction** : la règle officielle peut très bien être « à la date de fin de contrat », auquel cas le
code est juste et c'est le commentaire qui manque. Ne rien changer sans source.

**Action suggérée** : trancher la question sur le guide, puis soit documenter la règle, soit passer
au prorata.

---

### 13. La bannière « Règles à vérifier » ne peut jamais s'afficher

**Preuve** : `src/config/franceTravailConfig.ts:29` — `valableJusquau: null` ;
`src/components/TopBar.tsx:25` — `perime` vaut donc toujours `false`.

**Pourquoi c'est un risque** : le mécanisme d'avertissement sur la péremption des règles existe mais
est structurellement inerte. Le choix (« ne rien affirmer sans date officielle ») est cohérent et
documenté, mais le résultat net est qu'aucune alerte de péremption ne se déclenchera jamais — la
vigilance repose entièrement sur la routine mensuelle manuelle.

**Action suggérée** : soit assumer et retirer le mécanisme mort, soit le remplacer par un rappel basé
sur la date de dernière vérification effective.

---

### 14. La source affichée à l'utilisateur est périmée

**Preuve** : `src/config/franceTravailConfig.ts:18-22` — `dateEntreeVigueur: "2026-06-01"` et une
source citant le « Guide France Travail éd. **mars 2026** ». Or l'édition réellement en ligne et lue
le 03/08/2026 est celle de **juillet 2026**, et trois constantes ont été re-vérifiées le 03/08.
`src/components/TopBar.tsx:57` affiche ce texte en permanence.

**Pourquoi c'est un risque** : l'app annonce à l'utilisateur des règles « vérifiées au 01/06/2026 »
sur la foi d'une édition qui n'est plus la dernière. Pas de chiffre faux à la clé, mais une
information de fraîcheur inexacte, affichée en permanence.

**Action suggérée** : mettre `meta.version`, `dateEntreeVigueur` et `source` à jour à chaque
vérification réelle, et citer l'édition juillet 2026.

---

### 15. `dateUtils.ts` n'a aucun test dédié

**Preuve** : c'est le seul fichier de `src/engine/` sans `__tests__/<nom>.test.ts` correspondant
(vérifié par comparaison des deux listes).

**Pourquoi c'est un risque** : toute l'arithmétique de calendrier du projet (fenêtres, chevauchements,
découpage mensuel, âge) repose sur ces dix fonctions. Elles sont exercées indirectement par les
autres tests, mais aucun test ne cible leurs cas limites (mois de 28/31 jours, changements d'heure,
années bissextiles, intervalles inversés).

**Action suggérée** : un fichier de tests dédié, en particulier sur `joursChevauchement`,
`moisEntre` et `ageAuJour`.

---

### 16. ✅ RÉSOLU (03/08/2026) — Le total ARE annuel est recalculé par un second chemin, différent de celui affiché

**Preuve** : `src/lib/totalAreAnnuel.ts:23-44` — appelle `calculerSerieDepuisContrats` (moteur A)
pour obtenir la liste des mois, puis **jette** les jours indemnisés qu'il vient d'obtenir et les
recalcule avec `calculerSerie` (moteur B).

**Pourquoi c'est un risque** : c'est la même divergence qu'au point 3, sur un autre écran (le
pré-remplissage « Allocations ARE » du module Frais réels). L'impact est limité parce que la valeur
est explicitement présentée comme un pré-remplissage éditable, et le fichier le documente.

**Action suggérée** : à traiter en même temps que le point 3 — utiliser directement les jours
indemnisés du moteur A.

---

### 17. Un contrat EEE sans jours saisis compte zéro heure, en silence

**Preuve** : `src/engine/decompteHeures.ts:24-26` — pour un contrat `territoire: "eee_suisse_uk"`,
les heures valent `(contrat.nbJoursEEE ?? 0) × 6`. Rien dans le type
(`src/types/index.ts:26-28`, tous les champs optionnels) ni dans le schéma de stockage n'impose que
`nbJoursEEE` soit présent quand le territoire l'exige.

**Pourquoi c'est un risque** : un contrat importé ou construit sans ce champ disparaît du décompte
sans le moindre signal. L'erreur va dans le sens prudent (sous-comptage), ce qui la rend moins grave,
mais elle reste invisible.

**Action suggérée** : rendre l'invariant explicite dans le type (union discriminée sur `territoire`)
ou, à défaut, signaler tout contrat qui n'apporte aucune heure.

---

### 18. Deux règles réglementaires restent explicitement non vérifiées

**Preuve** : `src/engine/periodeReference.ts:95` (« vérifier si la borne est inclusive ou
exclusive ») et `src/engine/salaireReference.ts:19-23` (`suspension_contrat` non confirmé exclu du
salaire aménagé).

**Pourquoi c'est un risque** : les deux sont traités par un choix par défaut assumé et documenté, pas
par une source. Chacun peut décaler une fenêtre d'un jour ou modifier un salaire aménagé, donc un
montant. La probabilité est faible, la traçabilité est bonne.

**Action suggérée** : à verser à la routine mensuelle de vérification, pas un chantier en soi.

---

### 19. Le SMIC journalier utilisé par la franchise salaires est une valeur déduite, pas sourcée

**Preuve** : `src/config/franceTravailConfig.ts:252` et `:269` — `smicJournalierBrut` et son
historique sont marqués 🔶 « dérivé de smicHoraireBrut × 7 — à confirmer depuis une source
officielle ». Ils alimentent `calculerFranchiseSalaires`
(`src/engine/indemnisationMensuelle.ts:359`).

**Pourquoi c'est un risque** : la formule officielle dit « SMIC journalier », sans préciser que c'est
l'horaire × 7 — c'est une hypothèse du projet. **Impact actuellement nul** : depuis aujourd'hui, la
franchise salaires est déclarée par l'utilisateur et le chemin calculé n'est appelé par personne. Le
risque ne se matérialiserait qu'en rebranchant le calcul.

**Action suggérée** : sourcer la valeur avant tout rebranchement du calcul.

---

### 20. Durée d'amortissement d'un instrument non validée, mais utilisée dans un chiffre affiché

**Preuve** : `src/config/franceTravailConfig.ts:200` — `instrument: 5, // 🔶 cas d'espèce — durée à
valider avec le fisc`, consommée par le module Frais réels.

**Pourquoi c'est un risque** : cette durée détermine un montant de déduction fiscale présenté à
l'utilisateur. **À vérifier** : je n'ai pas contrôlé si l'écran Frais réels présente ce montant comme
une estimation ou comme un chiffre sûr — la gravité dépend entièrement de cela.

**Action suggérée** : vérifier la formulation à l'écran ; si le chiffre y est présenté sans réserve,
ajouter une mention explicite.

---

## Suivi des corrections

| Date | Point | État |
|---|---|---|
| 03/08/2026 | **1** — lecture ratée → effacement | ✅ résolu (`89c4d04`) |
| 03/08/2026 | **2** — échec d'écriture silencieux | ⚠️ filet minimal posé, sujet ouvert |
| 03/08/2026 | **3** — deux moteurs, ordres différents | ✅ résolu — **moteur unique** : `calculerSerie.ts` et `franchises.ts` supprimés. L'ordre est tranché par deux sources officielles (Annexe X art. 23 §1er ; guide FT p.12 et p.17 étape 6) **et** par deux relevés de Benoît |
| 03/08/2026 | **4** — franchise salaires annoncée non déduite | ✅ résolu sur la contradiction — le seul moteur restant connaît la franchise salaires, l'écran ne peut plus annoncer une déduction qu'un autre moteur ignore. Le TOTAL reste déclaré, non calculé (cf. point 19) |
| 03/08/2026 | **21** — mois d'ouverture partiel mal traité par les deux moteurs | ✅ résolu — mois calculé sur sa vraie fenêtre (`dateOuverture` → fin du mois). **674,93 € d'ARE faux retirés** sur janvier + février 2026. Les 4 mois certifiés sont reproduits sans écart |
| 03/08/2026 | **12 bis** — pas de filet minimal viable | ✅ sans objet — la fusion a été faite, il n'y a plus de filet à chercher |
| 03/08/2026 | **12 quater** — le test « certifié » ne valide pas le chemin de production | ✅ résolu — `src/lib/__tests__/lignesRevenusMensuels.test.ts` part de la notification réelle + 62 contrats et déroule toute la chaîne jusqu'aux lignes affichées, **sans solde pré-mâché**, contrôle négatif inclus |
| 03/08/2026 | **16** — total ARE annuel recalculé par un second chemin | ✅ résolu — `totalAreAnnuel.ts` lit directement le moteur unique |
| 03/08/2026 | **24** — un mois futur s'affichait sans aucune réserve | ✅ résolu — tout mois calculé est marqué « estimation » jusqu'à l'import du relevé (décision de Benoît). Avant : le badge disparaissait dès les franchises épuisées, soit ~9 000 € affichés sans réserve sur août 2026 → janvier 2027 |
| 03/08/2026 | **22** — perte de données : les contrats ont disparu de l'app | ✅ résolu — fusion `docs/cadence-fusion-2026-08-03.json` (62 contrats) **importée et vérifiée à l'écran** (588 h / 507 h), deux arbitrages tranchés, origine canonique fixée |
| 03/08/2026 | **5** — « Sécurité » affiché sur une simple projection | ✅ résolu — le vert est réservé à l'acquis (heures travaillées **ou** contrats déjà signés). Une projection affiche désormais **« En bonne voie »** en violet, et son message dit explicitement « rien n'est encore acquis » + les heures manquantes |
| 03/08/2026 | **6** — « Bloqué » affiché à 30 jours même s'il ne manque presque rien | ✅ résolu — « Bloqué » exige que l'écart soit **hors de portée du plafond de l'Annexe 10** (28 cachets × 12 h = 336 h/mois, déjà sourcé dans la config — aucun chiffre inventé). Le cas rattrapable affiche **« À rattraper »**. L'alerte critique « Échéance imminente » est conservée à l'identique, via un champ `echeanceImminente` séparé du badge |
| 03/08/2026 | **7** — fausse alerte « plafond de cachets dépassé » | ✅ résolu **en amont, pas dans le calcul** — règle tranchée par Benoît : un contrat ne couvre jamais deux mois civils, chaque mois se déclarant séparément à France Travail. Imposée à l'ÉCRITURE en un seul point (`lib/contratUnSeulMois.ts`, branché sur les 3 fonctions d'écriture d'`App.tsx` : formulaire, édition, import de bulletin, revue IA), **jamais à la lecture** (devoir n°1). `moisCle(contrat.date)` redevient exact par construction. Les deux pistes envisagées d'abord — prorata au jour, puis fourchette certain/possible — sont documentées et écartées dans `lib/contratUnSeulMois.ts` : la première inventait une règle non sourcée, la seconde gérait une ambiguïté qui ne doit pas exister |
| 03/08/2026 | **26** — le plafond de 28 cachets/mois ne plafonne rien | 🔴 NOUVEAU, non traité — trouvé en corrigeant le 7. `decoupageMensuel.ts:8-9` affirme qu'il gouverne l'affiliation aux 507 h ; `decompteHeures.ts` ne l'applique pas. Si la règle est le plafonnement, l'app surcompte les heures d'un mois chargé → faux « Sécurité ». **Non sourcé, ne rien coder avant d'avoir tranché** · portée nulle sur les données de Benoît (mois le plus chargé : 20 cachets) |
| 03/08/2026 | **23** — l'import est inaccessible avant d'avoir créé un profil | ✅ résolu — **aucun nouveau chemin d'écriture, et aucun « profil minimal » créé** : vérifié que `confirmerImport` teste `!donnees` et jamais `donnees.profil`, et que le schéma d'écriture déclare `profil` nullable. La dépendance était donc une pure contrainte d'affichage (un `return <Onboarding/>` anticipé). L'écran d'accueil propose désormais « Restaurer une sauvegarde », **avant** le formulaire, en réutilisant à l'identique la machinerie d'import existante (sauvegarde de sécurité → validation → écriture). Le modal ne brandit plus « Action irréversible » quand il n'y a rien à écraser — un avertissement sans objet est un faux avertissement. 4 tests d'intégration sur le vrai `App`, dont le refus d'un fichier invalide sans état partiel |
| 03/08/2026 | **25** — plafond de cumul 118 % du PMSS jamais appliqué | 🔴 NOUVEAU, non traité — surévalue l'ARE d'un mois à forte activité · **portée nulle sur les données actuelles de Benoît** (2 957 € contre 4 725,90 €) |
| 03/08/2026 | **14** — la source réglementaire affichée à l'utilisateur est périmée | ✅ résolu — deux défauts distincts, pas un. (1) `meta.source` citait l'édition **mars 2026** du guide alors que le projet travaillait sur celle de **juillet 2026** depuis fin juillet (`renouvellementAnticipe.ts:146`, `types/index.ts:150`, `docs/validation.md:182` « lue en entier ») : corrigé. (2) **Le libellé et le champ ne disaient pas la même chose** : « Règles vérifiées au … » affichait `meta.dateEntreeVigueur`, qui date en réalité l'entrée en vigueur du **SMIC configuré** (12,31 €, arrêté du 22 mai 2026) — fait confirmé par Benoît, et corroboré par `valeursDatees.smicHoraireBrut`. Un champ `meta.dateDerniereVerification` (fait déclaré, jamais recalculé) a été ajouté ; c'est la seule date affichée. `dateEntreeVigueur` est conservée mais **plus affichée** : l'exposer supposait d'affirmer ce qu'elle date. Même confusion dans `joursDepuisMiseAJourConfig` (annonçait « dernière mise à jour », comptait depuis l'entrée en vigueur) — **le calcul était faux, pas l'intention** : renommée `joursDepuisDerniereVerification`. Dates ISO nues (`2026-06-01`) désormais lisibles via `lib/dateLisible.ts`. ⚠️ **Vérification manuelle au navigateur non faite** — remplacée par `components/__tests__/bandeauRegles.test.tsx` (rend les deux composants et lit le texte affiché), avec **contrôle négatif exécuté** : l'ancien libellé remis dans `TopBar` fait rougir 2 tests |
| 03/08/2026 | **13** — la bannière « Règles à vérifier » ne peut jamais s'afficher | ✅ résolu — **arbitrage de Benoît : option B, supprimer plutôt que remplacer.** `meta.valableJusquau`, la fonction `estPerime` et les deux branches conditionnelles (`TopBar.tsx`, `MonProfil.tsx`) sont supprimés. Motif : `valableJusquau` valait `null` faute d'échéance officielle publiée, la bannière ne pouvait donc jamais s'allumer — l'app donnait l'illusion d'une veille automatique. Benoît fait la veille à la main et l'assume ; `docs/routine-mensuelle-veille.md` devient le **seul** filet et le dit explicitement (il exigeait jusqu'ici de mettre à jour `valableJusquau`, champ désormais inexistant — corrigé). L'option écartée était un champ déclaré `prochaineVerificationPrevue` : refusée, pas de bandeau automatique de plus à maintenir. ⚠️ Ne pas réintroduire de seuil de durée (« > 6 mois = périmé ») : ce serait revenir au `SEUIL_PEREMPTION_JOURS = 365` inventé, déjà supprimé une fois |
| 03/08/2026 | **9** — le texte de consentement IA affirme une chose pas encore vraie | 🔴 **reporté sciemment, pas oublié** — décision de Benoît le 03/08 : le texte reste **inchangé**, la bascule de `MISTRAL_API_KEY` sur le plan payant se fera **à la toute fin du projet**, et le point se clôt à ce moment-là. Motif accepté : Benoît est le seul utilisateur, les envois sont ses propres documents, et il sait ce qu'il envoie — l'affirmation inexacte n'est faite qu'à lui-même, en connaissance de cause. ⚠️ **La décision cesse de tenir dès qu'un autre utilisateur peut envoyer un document** : le canal IA est monté sans interrupteur (`App.tsx:501`), donc toute ouverture à des tiers (cf. point 8, qui interdit déjà le déploiement public en l'état) rend le texte faux envers quelqu'un qui n'a pas choisi. Ne PAS reformuler le texte d'ici là : ce n'est plus un oubli, c'est un choix tracé |

## Résumé

*(rédigé avant toute correction — 1 est depuis résolu, 2 partiellement, cf. le tableau ci-dessus)*

**11 points 🔴 et 9 points 🟡.** Le thème dominant est net : le projet est très rigoureux sur les
*formules* (chaque constante est sourcée, chaque écart documenté), mais beaucoup moins sur les
*chemins* — deux moteurs concurrents pour le même tableau, un écran qui annonce une déduction qu'il
n'applique pas, des seuils d'affichage (« Sécurité », « Bloqué ») qui traduisent une projection en
certitude, et un stockage sans filet. Autrement dit : le danger ne vient plus des règles France
Travail, il vient de la plomberie autour.

**Un seul 🔴 mérite d'être traité avant tous les autres : le point 1** (une lecture ratée du stockage
efface tout, immédiatement et sans action de l'utilisateur). C'est le seul dont les dégâts sont
irréversibles — un chiffre faux se corrige, des données effacées ne reviennent pas. Les points 3 et 4
viennent juste après, et se corrigent probablement ensemble.
